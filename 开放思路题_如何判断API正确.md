# 开放思路题 —— 你怎么判断这个 API 是正确的?

**考察:** 候选人对"接口正确"的理解层次、断言思路、框架中的断言组织方式
**形式:** 只给响应,不给断言要求,听候选人自己的想法
**时长:** 8–10 分钟
**适用:** 所有候选人,可横向比较

---

## 一、题面

把 create trade 的响应贴给候选人(可连同请求一起),只问一句:

> "这是我们建单接口的一个真实响应。**在你们的自动化里,你会怎么判断这个接口是正确的?平时断言是怎么做的?**"

**然后让对方说 3–5 分钟,不打断、不提示。**

记录两件事:
- 对方**自己**说到了哪些方面
- **先说什么、后说什么** —— 顺序反映思维习惯

### 响应材料

```json
{
  "code": 200,
  "status": "SUCCESS",
  "msg": "",
  "data": {
    "trade": {
      "id": "TRD-1789347329384020000",
      "basic": {
        "productId": "FX_TRF",
        "direction": "Buy",
        "premiumAmount": 0,
        "premiumCurrency": "USD",
        "premiumDate": "2026-09-16"
      },
      "instrument": {
        "TRF": {
          "delivery": {
            "DD_Physical": { "dd_roundings": null }
          },
          "notionals": [
            { "currency": "JPY", "amount": 146200000 },
            { "currency": "USD", "amount": 1000000.10 }
          ],
          "payoffDict": {
            "1": {
              "PD_BrokenLine": {
                "pdbrokenLine": [
                  { "Segment": { "low": "-Infinity", "high": 146.2, "slope": -1,
                                 "inclLow": false, "inclHigh": false } },
                  { "Segment": { "low": 146.2, "high": "Infinity", "slope": -1,
                                 "inclLow": true, "inclHigh": false } }
                ]
              }
            }
          }
        }
      }
    }
  }
}
```

---

## 二、听的框架:"正确"的五个层次

| 层次 | 内容 | 说到这一层说明什么 |
|---|---|---|
| **① 协议层** | HTTP 状态码、响应时间 | 基本功,几乎人人都会说 |
| **② 结构层** | 字段是否齐全、类型是否正确、有无多余字段;schema / 契约校验 | 有契约意识 |
| **③ 业务结果层** | body 中的 `code` / `status`;关键值是否与请求一致;业务规则(交易号格式、payoff 两段首尾相接、金额精度) | 理解接口的业务含义 |
| **④ 副作用层** | 用返回的交易号再查一次;查库确认落地;下游是否收到;重复提交会不会建出两笔 | **有端到端意识 —— 这一层是分水岭** |
| **⑤ 期望值来源** | 期望值从哪里来:请求中带入的、独立计算的、基线文件 | 最深的一层,理解"用什么来判断对" |

**另有一条横线:断言在框架中如何组织**
- 每个用例手写断言?
- 有公共断言方法、schema 校验、反序列化成对象、软断言?
- 动态字段如何统一处理?

**这能区分"写用例的人"和"搭框架的人"。**

---

## 三、追问梯子

> **原则:** 只问对方**没有说到**的部分,从宽到窄。
> 每一问本身都带有一定方向性,所以要**标记哪些是自发说出的、哪些是追问后才说出的** —— 两者分量不同。

### 1. 要具体(贯穿全程)

对方说"校验关键字段""做契约校验"之类的抽象表述时:

> "**具体是哪几个字段?怎么决定哪些要验、哪些不验?**"
>
> "**你们代码里是怎么写的?写一两行给我看。**"

**开放题最大的风险是对方讲得流畅但全是概念。** 听到抽象词,就要求落到具体。

### 2. 期望值来源

> "你判断这个值是对的,**期望值是从哪里来的?**"

### 3. 响应之外 ⭐ 最关键

> "**如果响应里每个字段都对,能说明这个接口就正确了吗?**"

看对方能否**自己**想到:落库、再查询、下游系统、重复提交、审计记录。

### 4. 动态字段

> "交易号、时间戳每次都不一样,**你们怎么处理?**"

### 5. 断言的组织方式

> "接口多、字段多,**你们框架里断言是每个用例手写,还是有别的做法?**"

### 6. 真实经历

> "**有没有遇到过断言全部通过、但接口其实有问题的情况?**"

有真实故事的人,通常对断言的局限有切身体会。

### 7. 校准题(可选,放在最后)

> ⚠️ 这两问非常具体,问了就相当于给了提示,**只在前面都问完后用来校准**。

> "这里金额是 1000000.10,**你们代码里用什么类型接这个值?**"
>
> "`dd_roundings` 是 null,**你们会怎么断言它?**"

**期望:**
- 金额:Decimal / BigDecimal,且在**解析阶段**就处理(不先经过 float / double)
- null:**区分字段缺失与值为 null** —— 很多取值方式(Python `dict.get()`、Rest Assured `jsonPath().get()`)对两者都返回 null,直接判断会误通过

---

## 四、评分

| 分数 | 表现 |
|---|---|
| **5** | 自发覆盖 4 层以上;**主动提到响应之外的验证**(再查一次 / 查库 / 幂等);能说清期望值来源;有真实的踩坑例子;说得出框架中断言的组织方式 |
| **4** | 自发覆盖 ①②③,追问后能说到 ④ |
| **3** | 状态码 + body 的 status + 挑几个字段比对;追问后能补充一部分 |
| **2** | 只说"看返回 200、status 是 SUCCESS" |
| **1** | 说不出断言思路 |

---

## 五、记录表

| 维度 | 自发说出 | 追问后说出 | 未提及 | 备注 |
|---|---|---|---|---|
| ① 协议层(状态码、响应时间) | ☐ | ☐ | ☐ | |
| ② 结构层(字段、类型、schema) | ☐ | ☐ | ☐ | |
| ③ body 的 code / status | ☐ | ☐ | ☐ | |
| ③ 关键值与请求一致 | ☐ | ☐ | ☐ | |
| ③ 业务规则(格式、payoff、金额) | ☐ | ☐ | ☐ | |
| ④ 再查询 / 查库 | ☐ | ☐ | ☐ | |
| ④ 幂等 / 重复提交 | ☐ | ☐ | ☐ | |
| ④ 下游 / 审计 | ☐ | ☐ | ☐ | |
| ⑤ 期望值来源 | ☐ | ☐ | ☐ | |
| 动态字段处理 | ☐ | ☐ | ☐ | |
| 框架中的断言组织 | ☐ | ☐ | ☐ | |
| 真实踩坑经历 | ☐ | ☐ | ☐ | |
| 校准:金额类型 | — | ☐ 正确 | ☐ 错误 | |
| 校准:缺失 vs null | — | ☐ 正确 | ☐ 错误 | |

**首先提到的层次:** ______________

**总分:** ______ / 5

---

## 六、面试官提醒

1. **不要让它变成"能扯"的题。** 只要听到抽象说法,就追一句"具体举个例子"或"写一两行给我看"。**能具体,才算数。**

2. **不要打断前 3 分钟。** 先听完整,再用追问梯子补。过早追问会把对方引到你想要的方向,记录就不干净了。

3. **与实战版编码题配合使用效果最好。** 先问这道开放题听思路;时间允许,再给《编码题_API响应断言_实战版》让对方写代码。

   **"说与做"是否一致,是最干净的判断依据:**

   | 开放题中说了 | 编码题中做到了吗 |
   |---|---|
   | 要看 body 的 status | ☐ 是 ☐ 只断言了 200 |
   | 金额要精确 | ☐ 用了 Decimal/BigDecimal ☐ 用了 float/double |
   | 要区分缺失与 null | ☐ 检查了 key 存在 ☐ 用了 `.get()` |
   | 不写死列表下标 | ☐ 按条件查找 ☐ 写死了 `[1]` |

4. **时间控制在 10 分钟内。** 开放题容易超时,到点就收。

---

## 七、横向比较

| 观察点 | 候选人 D | 候选人 E | 其他 |
|---|---|---|---|
| 首先提到的层次 | | | |
| 自发覆盖的层数 | | | |
| 是否自发想到响应之外(④) | | | |
| 期望值来源 | | | |
| 框架中断言的组织 | | | |
| 真实踩坑经历 | | | |
| 校准:金额类型 | | | |
| 校准:缺失 vs null | | | |
| 说与做是否一致(如做了编码题) | | | |
| **总分** | | | |
